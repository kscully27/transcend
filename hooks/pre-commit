#!/bin/bash

echo "Running Flutter analyze..."
output=$(flutter analyze 2>&1 | grep -v "^info")
exit_code=${PIPESTATUS[0]}

if [ $exit_code -ne 0 ]; then
    echo "Flutter analyze found issues:"
    echo "$output"
    exit 1
fi

echo "No issues found!"
exit 0 